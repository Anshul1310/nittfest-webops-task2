apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: nittfest
  labels:
    app: order-service
    app.kubernetes.io/part-of: nittfest
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
